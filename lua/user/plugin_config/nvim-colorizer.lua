require("colorizer").setup({
  filetypes = { "*" },
  user_default_options = {
    css = true,
    AARRGGBB = true,
    mode = "virtualtext",
    virtualtext = "■",
    always_update = true,
  }
})
